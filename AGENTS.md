## Guidance for AI Agents

- Use Tailwind utilities for UI updates; avoid introducing parallel styling systems.
- Do not run dev commands that starts server
- Do run check/build commands that validate code and catch errors without starting servers
- When installing dependencies, search the web for the latest compatible version
